secrets:
  DATABASE_PASSWORD:
    source: aws_ssm
    ref: /app/prod/db/password
  REDIS_PASSWORD:
    source: aws_ssm
    ref: /app/prod/cache:password
  API_TOKEN:
    source: aws_ssm
    ref: /services/payments/prod/api/token
